var RUM = RUM || {};
RUM.History = (function(window, document, $, _) {

    function init(){
        // TODO
    }

    function getData(){
        alert("Not Implemented!");
    }

    var HISTORY = {
        init: init,
        getData: getData
    }
    return HISTORY;

})(window, document, window.jQuery, window._);
